import { StyleSheet, Text, View,Button } from 'react-native';

export default DonationHistory=({navigation})=>{
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Donation Hoistory</Text>
      </View>
    );
  }